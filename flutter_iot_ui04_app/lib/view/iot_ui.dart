import 'package:flutter/material.dart';

class IotUi extends StatelessWidget {
  const IotUi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.pinkAccent,
    );
  }
}
